@startuml
title Упрощённая диаграмма классов "Частная клиника" (Agile Modeling)

class Пациент {
  idПациента: String
  ФИО: String
  телефон: String
}

class Администратор {
  idАдминистратора: String
  ФИО: String
  --
  +найтиМедКарту()
  +создатьМедКарту()
  +записатьНаПрием()
}

class Врач {
  idВрача: String
  ФИО: String
  специализация: String
}

class МедицинскаяКарта {
  номерКарты: String
  персональныеДанные: String
  медицинскиеДанные: String
}

class ЗаписьНаПрием {
  idЗаписи: String
  дата: Date
  время: Time
  статус: String
}

' --- Связи между сущностями ---

Пациент "1" -- "1" МедицинскаяКарта : имеет
Пациент "1" -- "0..*" ЗаписьНаПрием : записывается

Администратор "1" -- "0..*" ЗаписьНаПрием : оформляет
Врач "1" -- "0..*" ЗаписьНаПрием : принимает

' --- Ограничения предметной области ---

note right of МедицинскаяКарта
Медицинская карта создаётся,
если пациент отсутствует в системе
end note

note right of ЗаписьНаПрием
Запись создаётся только
администратором
и только на свободный слот
end note

@enduml
